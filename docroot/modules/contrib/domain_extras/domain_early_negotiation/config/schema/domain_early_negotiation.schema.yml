domain_early_negotiation.settings:
  type: config_object
  label: 'Domain Early Negotiation settings'
  mapping:
    priority:
      type: integer
      label: 'Middleware priority for early domain negotiation'
