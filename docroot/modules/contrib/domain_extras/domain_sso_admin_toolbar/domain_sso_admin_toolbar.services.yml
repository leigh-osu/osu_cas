services:
  Drupal\domain_sso_admin_toolbar\Hook\DomainSsoAdminToolbarHooks:
    arguments:
      $currentUser: '@current_user'
      $entityTypeManager: '@entity_type.manager'
      $domainNegotiationContext: '@domain.negotiation_context'
