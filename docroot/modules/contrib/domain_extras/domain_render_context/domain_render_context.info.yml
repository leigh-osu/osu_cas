name: 'Domain Render Context'
description: 'Renders out-of-band output (email, PDF, a queue item) as if the request had come from another domain, so links, per-domain configuration and path prefixes match that domain rather than the request doing the work.'
type: module
package: Domain
core_version_requirement: ^10.2 || ^11
dependencies:
  - domain:domain

# Information added by Drupal.org packaging script on 2026-08-07
version: '3.0.5'
project: 'domain_extras'
datestamp: 1786097847
