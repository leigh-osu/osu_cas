langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_tax_cas_section
    - node.type.page
    - taxonomy.vocabulary.type
id: node.page.field_tax_cas_section
field_name: field_tax_cas_section
entity_type: node
bundle: page
label: 'CAS Section'
description: 'Faces of AgSci and section tagging (Student, Faculty, Alumni, …) — D7 field_cas_section, which the feature-page consolidation had dropped.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      type: type
    sort:
      field: name
      direction: asc
    auto_create: false
    auto_create_bundle: cas_impact_areas
field_type: entity_reference
